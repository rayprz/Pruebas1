# Copy to .env.local and fill in. Never commit the real key.
# Used server-side only by src/app/api/classify/route.ts.
ANTHROPIC_API_KEY=

# Optional: override the model used for AI classification of low-confidence rows.
# Defaults to claude-opus-5.
# SPEND_CLASSIFIER_MODEL=claude-opus-5
